CREATE TABLE [dbo].[ClickRateTable] (
    [id]           INT NOT NULL,
    [category]     INT NOT NULL,
    [range_from]   INT NOT NULL,
    [range_to]     INT NOT NULL,
    [currentvalue] INT NOT NULL
);

